CREATE TABLE bears (
    id INTEGER NOT NULL,
    name TEXT NOT NULL,
    age INTEGER NOT NULL,
    sex TEXT NOT NULL,
    color TEXT NOT NULL,
    temperament TEXT NOT NULL,
    alive BOOLEAN NOT NULL
);